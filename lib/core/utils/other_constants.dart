import 'package:flutter/material.dart';

BorderRadius generalBorderRadius = BorderRadius.circular(16);
BorderRadius generalBorderRadius10 = BorderRadius.circular(10);

BoxConstraints prefixIconBoxConstraints = const BoxConstraints(
  minWidth: 2,
  minHeight: 2,
);
